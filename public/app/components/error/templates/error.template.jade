.login-validation-summary-errors(ng-if="ctrlError.service.errors && ctrlError.service.errors.length > 0")
    .validation-summary-errors
        alert(ng-repeat="alert in ctrlError.service.errors", type="danger", close="ctrlError.service.clearError(alert)") {{alert.Message}}