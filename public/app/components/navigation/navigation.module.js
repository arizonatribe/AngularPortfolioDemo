(function() {
  'use strict';
  /**
  	 * Module which handles the setup of the main layout and navigation panes
  	 * @namespace folio.navigation
  	 */
  angular.module('folio.navigation', ['ng']);
})();
